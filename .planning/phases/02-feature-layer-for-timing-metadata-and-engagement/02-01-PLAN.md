---
phase: 02-feature-layer-for-timing-metadata-and-engagement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/youtube_trends/__init__.py
  - src/youtube_trends/feature_layer.py
  - tests/test_feature_layer.py
autonomous: true
requirements:
  - FACT-01
  - FACT-02
must_haves:
  truths:
    - A reusable Phase 2 feature table exists on top of the Phase 1 video-country snapshot parquet.
    - Publish-time and lag fields keep the locked Phase 1 UTC and local-date semantics.
    - Title, description, and tag metadata features are engineered with simple documented definitions.
  artifacts:
    - path: src/youtube_trends/feature_layer.py
      provides: Phase 2 feature builder and parquet writer.
    - path: tests/test_feature_layer.py
      provides: Automated checks for timing, metadata, and persistence behavior.
  key_links:
    - from: data/processed/video_country_snapshot.parquet
      to: src/youtube_trends/feature_layer.py
      via: Snapshot parquet ingestion.
      pattern: write_video_country_features|publish_hour_utc|title_char_count
    - from: src/youtube_trends/feature_layer.py
      to: tests/test_feature_layer.py
      via: Pytest regression checks.
      pattern: build_video_country_features|trend_span_days_observed|avg_tag_length_chars
---

<objective>
Build the reusable timing and metadata feature layer that later checkpoints can consume without recalculating ad hoc helper columns.

Purpose: Lock the Phase 2 feature definitions early so later comparative analysis stays consistent.
Output: A tested `feature_layer.py` module, module exports, and pytest coverage for timing and metadata helpers.
</objective>

<execution_context>
@$HOME/.copilot/get-shit-done/workflows/execute-plan.md
@$HOME/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-feature-layer-for-timing-metadata-and-engagement/02-CONTEXT.md
@.planning/phases/01-canonical-dataset-performance-frame/01-CONTEXT.md
@.planning/phases/01-canonical-dataset-performance-frame/01-02-SUMMARY.md
@checkpoints/01_canonical_dataset.md
@copilot-instructions.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create the Phase 2 feature builder scaffold and contract tests</name>
  <files>src/youtube_trends/__init__.py, src/youtube_trends/feature_layer.py, tests/test_feature_layer.py</files>
  <read_first>.planning/phases/02-feature-layer-for-timing-metadata-and-engagement/02-CONTEXT.md
.planning/phases/01-canonical-dataset-performance-frame/01-CONTEXT.md
checkpoints/01_canonical_dataset.md
copilot-instructions.md</read_first>
  <behavior>
    - Test 1: publish-time derived fields stay explicitly UTC and preserve the existing whole-day lag semantics.
    - Test 2: metadata helpers add structural title, description, and tag features without dropping the Phase 1 fields.
    - Test 3: the feature writer persists a parquet artifact that downstream analysis can reload.
  </behavior>
  <acceptance_criteria>
    - `src/youtube_trends/feature_layer.py` defines `build_video_country_features(` and `write_video_country_features(`.
    - `tests/test_feature_layer.py` contains tests named `test_build_video_country_features_adds_timing_columns`, `test_build_video_country_features_adds_metadata_features`, and `test_write_video_country_features_persists_output`.
    - `src/youtube_trends/__init__.py` exports the new Phase 2 feature-layer helpers.
  </acceptance_criteria>
  <action>Create the Phase 2 feature builder module and tests first. The builder must consume the Phase 1 snapshot parquet contract, preserve the existing proxy and lag columns, derive UTC publish-time helpers, derive structural metadata helpers from normalized title, description, and tag fields, and expose a module entrypoint so `python -m youtube_trends.feature_layer` writes `data/processed/video_country_features.parquet`.</action>
  <verify>
    <automated>python -m pytest tests/test_feature_layer.py -q</automated>
  </verify>
  <done>The project has a tested Phase 2 feature-table scaffold that downstream analysis can import or regenerate from the snapshot parquet.</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/test_feature_layer.py -q` passes.
- `python -m youtube_trends.feature_layer` writes the Phase 2 feature parquet without re-reading raw CSVs.
</verification>

<success_criteria>

- A reusable feature table exists for the default `country + video_id` analysis grain.
- Timing and metadata fields are documented in code and tests rather than left implicit.
- Later phases can load one saved feature artifact instead of recalculating helper columns inside analysis files.
</success_criteria>

<output>
After completion, create `.planning/phases/02-feature-layer-for-timing-metadata-and-engagement/02-01-SUMMARY.md`
</output>

---
phase: 02-feature-layer-for-timing-metadata-and-engagement
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - src/youtube_trends/feature_layer.py
  - tests/test_feature_layer.py
  - tests/test_phase2_checkpoint_contract.py
  - checkpoints/02_feature_layer.md
autonomous: true
requirements:
  - FACT-03
must_haves:
  truths:
    - Engagement fields are transformed into comparable ratios or safe derived metrics for cross-video analysis.
    - The Phase 2 checkpoint documents timing semantics, metadata definitions, and engagement leakage guardrails.
    - The Phase 2 feature parquet is reproducible from the Phase 1 snapshot parquet.
  artifacts:
    - path: src/youtube_trends/feature_layer.py
      provides: Comparable engagement metrics and transformed outcome fields.
    - path: tests/test_phase2_checkpoint_contract.py
      provides: Machine-checkable methodology guardrails for the Phase 2 checkpoint.
    - path: checkpoints/02_feature_layer.md
      provides: Human-readable Phase 2 feature definitions and interpretation guardrails.
  key_links:
    - from: src/youtube_trends/feature_layer.py
      to: checkpoints/02_feature_layer.md
      via: Shared column names and timing semantics.
      pattern: engagement_rate_vs_first_trending_views|publish_hour_utc|post-trending context
    - from: checkpoints/02_feature_layer.md
      to: tests/test_phase2_checkpoint_contract.py
      via: Text-level contract checks.
      pattern: Interpretation guardrails|video duration is unavailable|python -m youtube_trends.feature_layer
---

<objective>
Finish Phase 2 by adding comparable engagement context features and documenting the full feature contract in a Markdown checkpoint.

Purpose: Make the feature layer reusable and explicit before Phase 3 starts cross-country comparisons.
Output: Engagement feature coverage in the builder, checkpoint contract tests, and a Phase 2 Markdown checkpoint.
</objective>

<execution_context>
@$HOME/.copilot/get-shit-done/workflows/execute-plan.md
@$HOME/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-feature-layer-for-timing-metadata-and-engagement/02-CONTEXT.md
@src/youtube_trends/feature_layer.py
@tests/test_feature_layer.py
@checkpoints/01_canonical_dataset.md
@copilot-instructions.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend the feature builder with engagement context metrics</name>
  <files>src/youtube_trends/feature_layer.py, tests/test_feature_layer.py</files>
  <read_first>src/youtube_trends/feature_layer.py
tests/test_feature_layer.py
.planning/phases/02-feature-layer-for-timing-metadata-and-engagement/02-CONTEXT.md
checkpoints/01_canonical_dataset.md</read_first>
  <behavior>
    - Test 1: engagement totals and rates are computed from the first trending snapshot counts.
    - Test 2: zero-view rows use safe null outputs instead of divide-by-zero failures.
    - Test 3: light transformed outcome fields are available for later descriptive analysis.
  </behavior>
  <acceptance_criteria>
    - `tests/test_feature_layer.py` contains `test_build_video_country_features_adds_safe_engagement_features`.
    - `src/youtube_trends/feature_layer.py` contains the literal column name `engagement_rate_vs_first_trending_views`.
    - `python -m pytest tests/test_feature_layer.py -q` exits `0`.
  </acceptance_criteria>
  <action>Extend the feature builder so it derives engagement totals, rates, and light transformed outcome fields from `first_trending_views`, `first_trending_likes`, `first_trending_dislikes`, and `first_trending_comment_count`. Document through code structure that these are post-trending context features rather than creator-controlled inputs.</action>
  <verify>
    <automated>python -m pytest tests/test_feature_layer.py -q</automated>
  </verify>
  <done>The Phase 2 feature table now includes comparable engagement context metrics with safe zero handling.</done>
</task>

<task type="auto">
  <name>Task 2: Create the Phase 2 Markdown checkpoint contract</name>
  <files>checkpoints/02_feature_layer.md, tests/test_phase2_checkpoint_contract.py</files>
  <read_first>src/youtube_trends/feature_layer.py
tests/test_feature_layer.py
.planning/phases/02-feature-layer-for-timing-metadata-and-engagement/02-CONTEXT.md
copilot-instructions.md</read_first>
  <acceptance_criteria>
    - `checkpoints/02_feature_layer.md` contains headings `Feature table artifact`, `Timing features`, `Metadata features`, `Engagement context features`, `Interpretation guardrails`, and `Re-run instructions`.
    - `checkpoints/02_feature_layer.md` contains the literal strings `publish_hour_utc`, `engagement_rate_vs_first_trending_views`, `post-trending context`, and `video duration is unavailable`.
    - `tests/test_phase2_checkpoint_contract.py` contains a test named `test_phase2_markdown_checkpoint_documents_feature_layer_guardrails`.
    - `python -m pytest tests/test_feature_layer.py tests/test_phase2_checkpoint_contract.py -q` exits `0`.
  </acceptance_criteria>
  <action>Create `checkpoints/02_feature_layer.md` and `tests/test_phase2_checkpoint_contract.py`. The Markdown checkpoint must document the Phase 2 output parquet, the timing feature semantics, the metadata feature definitions, the engagement leakage guardrails, the unresolved category-label limitation, and the explicit note that `video duration is unavailable` in the current dataset. Include the rerun command for `python -m youtube_trends.feature_layer`.</action>
  <verify>
    <automated>python -m pytest tests/test_feature_layer.py tests/test_phase2_checkpoint_contract.py -q && python -m youtube_trends.feature_layer</automated>
  </verify>
  <done>The Phase 2 feature contract is readable in Markdown, machine-checkable in pytest, and reproducible by command.</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/test_feature_layer.py tests/test_phase2_checkpoint_contract.py -q` passes.
- `python -m youtube_trends.feature_layer` regenerates `data/processed/video_country_features.parquet` from the Phase 1 snapshot parquet.
- The Phase 2 checkpoint keeps timing semantics, leakage caveats, and missing-duration limits explicit.
</verification>

<success_criteria>

- Engagement context metrics are comparable across videos without divide-by-zero failures.
- The feature-layer methodology is explicit enough for future checkpoints and the final report to reuse safely.
- Phase 3 can start from one saved feature parquet and one Markdown checkpoint instead of reverse-engineering the Phase 2 logic.
</success_criteria>

<output>
After completion, create `.planning/phases/02-feature-layer-for-timing-metadata-and-engagement/02-02-SUMMARY.md`
</output>

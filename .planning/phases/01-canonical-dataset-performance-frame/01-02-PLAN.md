---
phase: 01-canonical-dataset-performance-frame
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/youtube_trends/performance_frame.py
  - tests/test_performance_frame.py
  - tests/test_phase1_checkpoint_contract.py
  - checkpoints/01_canonical_dataset.md
autonomous: true
requirements:
  - DATA-03
must_haves:
  truths:
    - A default video-country snapshot table exists and uses the first trending observation as its anchor.
    - The primary performance proxy is explicitly named as a trending-corpus proxy and computed per country/video pair.
    - The Phase 1 Markdown checkpoint explains the proxy, the no-causality rule, and the missing-duration limitation.
  artifacts:
    - path: src/youtube_trends/performance_frame.py
      provides: Video-country snapshot builder and explicit proxy computation.
    - path: tests/test_performance_frame.py
      provides: Automated checks for anchor-row and proxy logic.
    - path: checkpoints/01_canonical_dataset.md
      provides: Markdown checkpoint documentation of the canonical dataset contract and performance frame.
  key_links:
    - from: src/youtube_trends/canonical_dataset.py
      to: src/youtube_trends/performance_frame.py
      via: Row-level parquet input to the video-country snapshot builder.
      pattern: trending_rows.parquet|video_country_snapshot.parquet
    - from: src/youtube_trends/performance_frame.py
      to: tests/test_performance_frame.py
      via: pytest checks for anchor selection and proxy computation.
      pattern: build_video_country_snapshot|trend_days_in_country_proxy
    - from: src/youtube_trends/performance_frame.py
      to: checkpoints/01_canonical_dataset.md
      via: Markdown checkpoint narrative and regeneration steps.
      pattern: trend_days_in_country_proxy|Limitations
---

<objective>
Create the default video-country snapshot and Markdown checkpoint documentation that turn the row-level dataset into an explicitly framed performance foundation.

Purpose: Finish Phase 1 by encoding the proxy logic and documenting its limits in a Markdown checkpoint deliverable.
Output: A tested `performance_frame.py` module, Markdown checkpoint contract tests, and a Markdown checkpoint that explains the proxy and regeneration path.
</objective>

<execution_context>
@$HOME/.copilot/get-shit-done/workflows/execute-plan.md
@$HOME/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-canonical-dataset-performance-frame/01-CONTEXT.md
@.planning/phases/01-canonical-dataset-performance-frame/01-RESEARCH.md
@project.md
@copilot-instructions.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement the video-country snapshot and explicit proxy logic</name>
  <files>src/youtube_trends/performance_frame.py, tests/test_performance_frame.py</files>
  <read_first>src/youtube_trends/canonical_dataset.py
tests/test_canonical_dataset.py
.planning/phases/01-canonical-dataset-performance-frame/01-CONTEXT.md
.planning/phases/01-canonical-dataset-performance-frame/01-RESEARCH.md
project.md</read_first>
  <behavior>
    - Test 1: the snapshot table keeps the earliest `trending_date_local` row as the anchor for each `country` + `video_id` pair.
    - Test 2: `trend_days_in_country_proxy` equals the number of row-level trending observations for that `country` + `video_id` pair.
    - Test 3: `publish_to_first_trend_days` is stored as an integer day difference derived from the UTC publish date and first local trending date.
    - Test 4: the snapshot builder never collapses multi-country videos into one global row.
  </behavior>
  <acceptance_criteria>
    - `src/youtube_trends/performance_frame.py` defines `build_video_country_snapshot(` and `write_video_country_snapshot(`.
    - `src/youtube_trends/performance_frame.py` contains the literal column name `trend_days_in_country_proxy`.
    - `tests/test_performance_frame.py` contains tests named `test_first_trending_snapshot_is_anchor`, `test_trend_days_proxy_counts_rows_per_country_video`, and `test_publish_to_first_trend_days_is_integer_days`.
  </acceptance_criteria>
  <action>Create `src/youtube_trends/performance_frame.py` and `tests/test_performance_frame.py`. Implement `build_video_country_snapshot(rows_df)` so it groups by the concrete key pair `country` and `video_id`, sorts by `trending_date_local` ascending, keeps the first row as the anchor row, computes `first_trending_date_local`, `last_trending_date_local`, `trend_days_in_country_proxy`, `publish_to_first_trend_days`, `first_trending_views`, `first_trending_likes`, `first_trending_dislikes`, and `first_trending_comment_count`, and preserves `title`, `channel_title`, `category_id`, `tags`, and `description` from the anchor row. Add `write_video_country_snapshot(input_path='data/processed/trending_rows.parquet', output_path='data/processed/video_country_snapshot.parquet')` plus `main()` so `python -m youtube_trends.performance_frame` regenerates the snapshot output. Do not create a global cross-country video table in Phase 1.</action>
  <verify>
    <automated>python -m pytest tests/test_performance_frame.py -q</automated>
  </verify>
  <done>The project has a tested default analysis table keyed by `country` + `video_id`, anchored on the first trending snapshot, with an explicit primary proxy column.</done>
</task>

<task type="auto">
  <name>Task 2: Create the Phase 1 Markdown checkpoint contract and proxy documentation</name>
  <files>checkpoints/01_canonical_dataset.md, tests/test_phase1_checkpoint_contract.py, src/youtube_trends/performance_frame.py</files>
  <read_first>src/youtube_trends/performance_frame.py
src/youtube_trends/canonical_dataset.py
.planning/phases/01-canonical-dataset-performance-frame/01-CONTEXT.md
.planning/phases/01-canonical-dataset-performance-frame/01-RESEARCH.md
project.md
copilot-instructions.md</read_first>
  <acceptance_criteria>
    - `checkpoints/01_canonical_dataset.md` contains headings `Raw data inventory`, `Canonical row-level table`, `Video-country snapshot table`, `Trending-corpus performance proxy`, `Limitations`, and `Re-run instructions`.
    - `checkpoints/01_canonical_dataset.md` contains the literal strings `trend_days_in_country_proxy`, `association`, and `video duration is unavailable`.
    - `tests/test_phase1_checkpoint_contract.py` contains a test named `test_phase1_markdown_checkpoint_documents_proxy_and_limitations`.
    - `python -m pytest tests/test_performance_frame.py tests/test_phase1_checkpoint_contract.py -q` exits `0`.
  </acceptance_criteria>
  <action>Create `checkpoints/01_canonical_dataset.md` and `tests/test_phase1_checkpoint_contract.py`. The Markdown checkpoint must document `data/processed/trending_rows.parquet` and `data/processed/video_country_snapshot.parquet`, show the row-level versus video-country contract, document `trend_days_in_country_proxy` as the primary trending-corpus proxy, state that the project reports associations inside the trending corpus rather than causes, and include the explicit limitation statement `video duration is unavailable in the provided dataset`. Include rerun commands for `python -m youtube_trends.canonical_dataset` and `python -m youtube_trends.performance_frame`. Add `tests/test_phase1_checkpoint_contract.py` to read the Markdown text and assert the required headings and proxy/limitation strings are present. Do not add category-label mapping, API enrichment, or sentiment analysis in this phase.</action>
  <verify>
    <automated>python -m pytest tests/test_performance_frame.py tests/test_phase1_checkpoint_contract.py -q && python -m youtube_trends.performance_frame</automated>
  </verify>
  <done>The Phase 1 Markdown checkpoint documents the canonical dataset contract, the primary proxy, rerun steps, and the required limitations in a machine-checkable way.</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/test_performance_frame.py tests/test_phase1_checkpoint_contract.py -q` passes.
- `python -m youtube_trends.performance_frame` regenerates `data/processed/video_country_snapshot.parquet` from the row-level dataset.
- The Markdown checkpoint explicitly states the proxy, the no-causality framing, and the missing-duration limitation.
</verification>

<success_criteria>

- The default analysis table is derived from the row-level dataset and keyed by `country` + `video_id`.
- The primary performance target is explicit, reproducible, and documented as a trending-corpus proxy.
- Phase 1 ends with a Markdown checkpoint that future phases can reference for dataset contract and methodology.
</success_criteria>

<output>
After completion, create `.planning/phases/01-canonical-dataset-performance-frame/01-02-SUMMARY.md`
</output>

---
phase: 01-canonical-dataset-performance-frame
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - requirements.txt
  - src/youtube_trends/__init__.py
  - src/youtube_trends/canonical_dataset.py
  - tests/test_canonical_dataset.py
autonomous: true
requirements:
  - DATA-01
  - DATA-02
must_haves:
  truths:
    - The project can build one harmonized row-level dataset from all 10 country CSV files.
    - Row-level canonical data preserves raw values while exposing normalized date, boolean, and text helper fields.
    - The row-level dataset can be regenerated by command instead of manual spreadsheet work.
  artifacts:
    - path: requirements.txt
      provides: Reproducible dependency list for the phase pipeline.
    - path: src/youtube_trends/canonical_dataset.py
      provides: Canonical ingestion, normalization, and row-level parquet writer.
    - path: tests/test_canonical_dataset.py
      provides: Automated checks for schema, parsing, and normalization.
  key_links:
    - from: data/*.csv
      to: src/youtube_trends/canonical_dataset.py
      via: CSV discovery and per-country ingestion.
      pattern: country|build_trending_rows|parse_trending_date
    - from: src/youtube_trends/canonical_dataset.py
      to: tests/test_canonical_dataset.py
      via: pytest contract tests.
      pattern: parse_trending_date|normalize_bool|normalize_placeholder_text
---

<objective>
Build the reproducible row-level canonical dataset pipeline and validation harness that every later phase will consume.

Purpose: Lock the ingestion and normalization contract for all country CSVs before any proxy logic or analysis builds on top of it.
Output: `requirements.txt`, a canonical row-level pipeline module, and tests that prove the row-level dataset contract.
</objective>

<execution_context>
@$HOME/.copilot/get-shit-done/workflows/execute-plan.md
@$HOME/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-canonical-dataset-performance-frame/01-CONTEXT.md
@.planning/phases/01-canonical-dataset-performance-frame/01-RESEARCH.md
@project.md
@copilot-instructions.md
@data/USvideos.csv
@data/JPvideos.csv
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create the row-level pipeline scaffold and test contract</name>
  <files>requirements.txt, src/youtube_trends/__init__.py, src/youtube_trends/canonical_dataset.py, tests/test_canonical_dataset.py</files>
  <read_first>.planning/phases/01-canonical-dataset-performance-frame/01-CONTEXT.md
.planning/phases/01-canonical-dataset-performance-frame/01-RESEARCH.md
project.md
data/USvideos.csv
data/JPvideos.csv
copilot-instructions.md</read_first>
  <behavior>
    - Test 1: `parse_trending_date('17.14.11')` returns the local calendar date `2017-11-14`.
    - Test 2: `normalize_bool` maps `TRUE`, `True`, `FALSE`, and `False` to booleans case-insensitively.
    - Test 3: placeholder text such as `[none]` or blank description sets missing flags without deleting the raw source fields.
    - Test 4: discovery finds all 10 files in `data/*.csv` and assigns the correct two-letter country code from the filename prefix.
  </behavior>
  <acceptance_criteria>
    - `requirements.txt` contains `pandas`, `pyarrow`, `pandera`, `pytest`, `jupyterlab`, and `ipykernel`.
    - `src/youtube_trends/canonical_dataset.py` defines `parse_trending_date(`, `normalize_bool(`, `normalize_placeholder_text(`, and `build_trending_rows(`.
    - `tests/test_canonical_dataset.py` contains tests named `test_parse_trending_date_yy_dd_mm`, `test_normalize_bool_case_insensitive`, `test_placeholder_text_sets_missing_flags`, and `test_all_country_files_are_discovered`.
  </acceptance_criteria>
  <action>Create `requirements.txt` with the exact package names `pandas`, `pyarrow`, `pandera`, `pytest`, `jupyterlab`, and `ipykernel`. Create `src/youtube_trends/__init__.py` and `src/youtube_trends/canonical_dataset.py`. In `canonical_dataset.py`, add the concrete functions `discover_country_files(data_glob: str = 'data/*.csv')`, `parse_trending_date(value: str)`, `normalize_bool(value)`, `normalize_placeholder_text(value, placeholder='[none]')`, and `build_trending_rows(input_glob: str = 'data/*.csv', output_path: str = 'data/processed/trending_rows.parquet')`. Preserve raw fields `title`, `tags`, and `description`; add helper columns `title_normalized`, `tags_normalized`, `description_normalized`, `tags_is_missing`, `description_is_missing`, `tags_list`, and `tag_count`. Write `tests/test_canonical_dataset.py` first, covering the four behaviors above before implementing the functions.</action>
  <verify>
    <automated>python -m pytest tests/test_canonical_dataset.py -q</automated>
  </verify>
  <done>The project has a test-backed scaffold for country discovery, date parsing, boolean normalization, and raw-plus-normalized text handling.</done>
</task>

<task type="auto">
  <name>Task 2: Implement the canonical row-level dataset builder</name>
  <files>src/youtube_trends/canonical_dataset.py, tests/test_canonical_dataset.py</files>
  <read_first>src/youtube_trends/canonical_dataset.py
tests/test_canonical_dataset.py
.planning/phases/01-canonical-dataset-performance-frame/01-CONTEXT.md
.planning/phases/01-canonical-dataset-performance-frame/01-RESEARCH.md
data/USvideos.csv
data/JPvideos.csv</read_first>
  <acceptance_criteria>
    - `src/youtube_trends/canonical_dataset.py` contains the output paths `data/processed/trending_rows.parquet` and `data/processed/source_manifest.csv`.
    - `src/youtube_trends/canonical_dataset.py` writes a `country` column and a parsed `trending_date_local` column while keeping `publish_time` in UTC form.
    - `src/youtube_trends/canonical_dataset.py` writes normalized boolean columns for `comments_disabled`, `ratings_disabled`, and `video_error_or_removed`.
    - `python -m pytest tests/test_canonical_dataset.py -q` exits `0`.
  </acceptance_criteria>
  <action>Implement `build_trending_rows` so it reads every file matched by `data/*.csv`, derives `country` from the filename prefix (for example `USvideos.csv` -> `US`), validates the shared 16-column schema, parses `trending_date` into a date field named `trending_date_local`, parses `publish_time` with UTC awareness, normalizes the three boolean flag columns case-insensitively, preserves the raw text fields, adds the helper text columns and missing-value flags from Task 1, and writes the harmonized row-level output to `data/processed/trending_rows.parquet`. Also write `data/processed/source_manifest.csv` with one row per source file containing at least `country`, `source_file`, and `row_count`. Add a `main()` entrypoint so `python -m youtube_trends.canonical_dataset` regenerates both outputs without manual editing.</action>
  <verify>
    <automated>python -m pytest tests/test_canonical_dataset.py -q && python -m youtube_trends.canonical_dataset</automated>
  </verify>
  <done>The row-level canonical dataset can be regenerated from the raw CSV files, all 10 countries are represented, and the normalization contract is enforced by automated tests.</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/test_canonical_dataset.py -q` passes.
- `python -m youtube_trends.canonical_dataset` regenerates the row-level parquet and source manifest without manual edits.
- The row-level output keeps raw text fields while adding helper columns required by D-13 to D-16.
</verification>

<success_criteria>
- The phase produces one harmonized row-level dataset from all 10 country CSV files.
- Dates, booleans, and placeholder text are standardized consistently without discarding raw values.
- Later phases can consume one canonical row-level output instead of re-reading raw CSVs directly.
</success_criteria>

<output>
After completion, create `.planning/phases/01-canonical-dataset-performance-frame/01-01-SUMMARY.md`
</output>
